//modules
// import UserModule from '@/modules/user';
// import CRMModule from '@/modules/crm';
// import HRMModule from '@/modules/hrm';
// import WMSModule from '@/modules/wms';
// import FinanceModule from '@/modules/finance';
// import SettingsModule from '@/modules/settings';
// import CollectionModule from '@/modules/collection';

export default function (app, router) {
    // UserModule(app, router)
    // CRMModule(app, router)
    // HRMModule(app, router)
    // WMSModule(app, router)
    // SettingsModule(app, router)
    // FinanceModule(app, router)
    // CollectionModule(app, router)
}